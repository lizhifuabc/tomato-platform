server:
  port: 9000

logging:
  level:
    root: INFO
    org.springframework.web: INFO
    org.springframework.security: INFO
    org.springframework.security.oauth2: INFO

tomato:
  auth:
    successForwardUrl: /login/success

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/tomato_oauth?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: tomato
    password: tomato