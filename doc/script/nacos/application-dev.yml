# 日志模式
logging:
  pattern:
    level: '%5p [${spring.application.name:},%X{traceId:-},%X{spanId:-}]'
# 暴露监控端点
management:
  endpoints:
    web:
      exposure:
        ## yml文件中存在特殊字符，必须用单引号包含，否则启动报错
        include: '*'
  # trace 配置（100%）
  tracing:
    sampling:
      probability: 1.0